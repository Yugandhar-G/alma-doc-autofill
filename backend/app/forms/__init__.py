"""Immigration forms registry — USCIS knowledge as data.

Mirrors the screener's criteria-registry pattern: which forms each visa
classification needs is DATA (validated, versioned, cited to official form
pages), never model output. The form library downloader stores the official
blank PDFs locally so packages can populate them offline.
"""
